apiVersion: v1
kind: ConfigMap
metadata:
  name: server-app-h2
  namespace: default
  labels:
    app: server-app-h2
    group: sample
data:
  SPRING_PROFILES_ACTIVE: "kube"
  HEAP_SIZE: "256m"
  META_SIZE: "300m"
  SERVER_TRACING_PROBABILITY: "1.0"
