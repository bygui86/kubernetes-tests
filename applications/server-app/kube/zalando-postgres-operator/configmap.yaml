apiVersion: v1
kind: ConfigMap
metadata:
  name: server-app
  namespace: default
  labels:
    app: server-app
    group: sample
data:
  SPRING_PROFILES_ACTIVE: "kube-pg-oper"
  HEAP_SIZE: "256m"
  META_SIZE: "300m"
  POSTGRES_SCHEMA: "foo"
  SERVER_TRACING_PROBABILITY: "1.0"
